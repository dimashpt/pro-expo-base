{
  "general": {
    "error": {
      "session_expired": "Session expired"
    }
  },
  "validation": {
    "required": "Field is required",
    "invalid": "Invalid input",
    "minimum": "Field must be at least {{minimum}} characters",
    "startsWith": "Field must start with {{startsWith}}",
    "email": "Invalid email address"
  }
}
